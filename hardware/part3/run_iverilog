#!/usr/bin/env bash
set -euo pipefail

# Build and run the Part 3 core testbench in-place
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR"

iverilog -g2012 -s core_tb -o core_tb.vvp verilog/*.v
vvp core_tb.vvp
